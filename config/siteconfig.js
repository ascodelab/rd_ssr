const siteconfig = {
    siteName: 'Rati Digital',
    // Disqus
    disqus: {
        on: true,
        loadingStrategy: 'button', // Options: onload, lazy, button
        siteShortName: 'ratidigital-in'
    },
}

export default siteconfig;